# configure only tasks
---
- name: install nginx
  apt:
    name: nginx
    state: present
    update_cache: yes
    notify: restart nginx

- name: ensure nginx enabled
  service:
    name: nginx
    state: started
    enabled: yes
